VM Name: dev-onboarding-etc
username: dev-cara
passowrd: 12345 (change this!)

Instructions for monitor.sh: This automatically runs every minute, but if you must use it, do ./monitor.sh. This will update the system.log file in /logs

Instructions for timesheet.sh: Run this at the end of the workday to log your hoursing using ./timesheet.sh. It will update the timesheet.log file in /logs
